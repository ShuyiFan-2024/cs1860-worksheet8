CHIP Splitter{
    IN in[16];
    OUT out1,out2,out3,out4;

    PARTS :
        Or(a=in[0],b=false,out=out1);
        Or(a=in[1],b=false,out=out2);
        Or(a=in[2],b=false,out=out3);
        Or(a=in[3],b=false,out=out4);
}